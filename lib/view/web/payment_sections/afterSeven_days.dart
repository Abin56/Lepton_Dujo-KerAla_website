

import 'package:flutter/material.dart';

class AfterSevenDaysScreen extends StatelessWidget {
  const AfterSevenDaysScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}